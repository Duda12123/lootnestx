// Types re-exported from the MDX content system
// Runtime data (products array, getProductBySlug, etc.) must be
// imported from @/lib/content in server components only.
// Use import type for type-only imports in client components.
export type { Product } from "./content"